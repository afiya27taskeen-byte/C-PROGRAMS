#include <stdio.h>

int main() {
    int a,b;
    int difference;
    printf("Enter any two integers");
    scanf("%d %d",&a,&b);
    difference = a-b ;
    printf("sum of three integers : %d " , difference);

    return 0;
}

